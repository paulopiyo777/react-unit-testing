import React from "react";
import { shallow } from "enzyme";
import Header from "./index";

// testthat checks whether functional component renders correctly
it("It should render without errors", () => {
  expect(1).toBe(1);
});
